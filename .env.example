# Copy to .env, then run `phantom init` and `phantom add VAR_NAME`.
# .env should contain Phantom tokens (phm_…), not plaintext secrets.
#
# Variables used by this project (fill as secrets are added):
# EXAMPLE_API_KEY=
